# NSD v19 — Python dependencies
# Install on Pi: pip install -r requirements.txt
# System prerequisite: sudo apt-get install rtl-sdr librtlsdr-dev

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9

# Rate limiting
slowapi==0.1.9

# RF / signal processing
numpy==1.26.4
pyrtlsdr==0.3.0
scipy==1.13.1

# PDF report generation
reportlab==4.2.5

# WebSocket support (uvicorn[standard] includes websockets, explicit pin for clarity)
websockets==12.0

# Phase 2 — ML classifier (pre-install for future use)
scikit-learn==1.5.1
joblib==1.4.2
